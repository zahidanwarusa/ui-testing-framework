<?xml version="1.0" encoding="UTF-8"?>
<extentreports>
    <configuration>
        <!-- report theme -->
        <theme>standard</theme>

        <!-- document encoding -->
        <encoding>UTF-8</encoding>

        <!-- protocol for script and stylesheets -->
        <protocol>https</protocol>

        <!-- title of the document -->
        <documentTitle>UI Testing Framework - Test Report</documentTitle>

        <!-- report name - displayed at top-nav -->
        <reportName>UI Testing Framework - Test Report</reportName>

        <!-- timestamp format -->
        <timeStampFormat>MMM dd, yyyy HH:mm:ss</timeStampFormat>

        <!-- custom CSS -->
        <styles>
            <![CDATA[
                .report-name { padding-left: 10px; }
                .report-name > img { float: left;height: 90%;margin-left: 30px;margin-top: 2px;width: auto; }
                .report-headline { color: #333; font-size: 18px; margin-bottom: 0; padding-left: 10px; }
                .report-banner { padding-top: 5px; }
                .badge.badge-primary { background-color: #5a9bbd; }
                img.r-img { width: 100%; max-width: 700px; }
            ]]>
        </styles>
    </configuration>
</extentreports>